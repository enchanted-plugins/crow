{
  "description": "Crow trust-scorer: content-detector red-flag scan after every file change",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/post-tool-use/score-change.sh\"",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
